# Shared

This directory is reserved for code, prompts, schemas, or utilities that are used by both the model and ontology areas.

Keep this directory intentionally small. If something is only used by one area, it should stay inside that area.
