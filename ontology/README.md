# Ontology

This directory is reserved for the semiconductor ontology and knowledge-graph assets that will be merged from `aitomatic/semicont`.

Planned contents:

- RDF/OWL vocabularies
- SHACL shapes
- example datasets
- documentation for ontology structure and usage

Until that merge lands, the active code and documentation remain under `model/`.
