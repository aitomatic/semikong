# Workflow: Session Start

1. Read `ontologist/AGENTS.md`.
2. Read `ontology/README.md`.
3. Read `ontologist/04-context/STATE.md` and `TASKS.md`.
4. Claim an item in `ontologist/04-context/SESSIONS.md`.
5. Run `python ontologist/05-analytics/tools/ontology_audit.py` to get baseline.
6. Execute one scoped task end-to-end.
