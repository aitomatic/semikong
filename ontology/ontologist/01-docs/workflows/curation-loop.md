# Workflow: Curation Loop

1. Select top `Ready` task.
2. Model/edit TTL in target ontology module.
3. Re-run structural audit.
4. Update context docs (`WORKLOG`, `STATE`, `TASKS`, `SESSIONS`).
5. Capture new policy decisions if needed.
